package com.bhavishay.coronatracker.ui.countryList

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.ViewModel
import com.bhavishay.coronatracker.repository.WorldStatsRepository


class CountryListViewModel(application:Application) : AndroidViewModel(application) {
//    private val repository : WorldStatsRepository()
}
